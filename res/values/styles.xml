<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="square_button">
        <item name="android:background">@drawable/back</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textSize">15dip</item>
        <item name="android:width">110dip</item>
        <item name="android:height">30dip</item>
    </style>

    <style name="square_rect">
        <item name="android:background">@drawable/back</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textSize">15dip</item>
    </style>

    <style name="square_edit">
        <item name="android:background">@drawable/back</item>
        <item name="android:layout_height">35dip</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">15dip</item>
        <item name="android:padding">5dip</item>
    </style>

    <style name="user_edit">
        <item name="android:background">@drawable/back_user</item>
        <item name="android:layout_height">55dip</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">20dip</item>
        <item name="android:padding">5dip</item>
    </style>

    <style name="match_edit">
        <item name="android:background">@drawable/back_pwd</item>
        <item name="android:layout_height">55dip</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:textSize">20dip</item>
        <item name="android:padding">5dip</item>
    </style>
    
    <style name="text_button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>